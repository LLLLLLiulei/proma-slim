## 1. 后端项目摘要与删除能力

- [x] 1.1 为 `page-builder` 首页新增项目摘要查询服务，聚合 `page-builder` 工作区、最近会话、预览入口和最近活跃时间
- [x] 1.2 新增 page-builder 首页历史记录专用 HTTP 路由，返回按最近活跃时间倒序排列的项目卡片摘要列表
- [x] 1.3 新增 page-builder 项目级联删除服务，删除目标工作区下全部会话、会话目录、消息文件、工作区根目录与 `workspace-files`
- [x] 1.4 新增 page-builder 项目删除专用 HTTP 路由，并保持普通工作区现有删除语义不变

## 2. 前端数据接入与首页挂载

- [x] 2.1 在 `apps/app` 或共享前端 API 层中补充 page-builder 历史列表查询与项目删除请求方法
- [x] 2.2 为 `apps/page-builder` 新增首页历史记录数据 hook 或状态封装，处理加载、空态、删除后刷新与编辑补建会话逻辑
- [x] 2.3 在不改动首页现有 hero 区结构和样式的前提下，将独立历史区组件挂载到首页对话框下方
- [x] 2.4 明确历史区视觉约束，确保新增区域延续首页现有风格，并将 `taste-skill` 作为历史区设计参考

## 3. 独立历史记录组件与卡片交互

- [x] 3.1 新增独立的历史区组件，负责渲染 `page-builder` 项目卡片列表而不是把逻辑堆入 `HomePage`
- [x] 3.2 新增独立的历史卡片组件，使用 `iframe` 懒加载预览，并在无预览时展示统一空态
- [x] 3.3 按首页现有视觉语言实现历史区容器、卡片边框、层次和 hover 态，避免引入割裂风格
- [x] 3.4 实现卡片悬浮蒙层及 `预览`、`编辑`、`删除` 三个操作按钮
- [x] 3.5 实现 `编辑` 动作优先恢复最近会话、缺失时自动创建新会话再跳转 builder 的流程
- [x] 3.6 实现 `删除` 动作的确认交互，并在删除成功后从首页历史区移除对应卡片

## 4. 测试与回归验证

- [x] 4.1 为后端项目摘要聚合、排序、无预览场景和级联删除逻辑补充单元或路由测试
- [x] 4.2 为首页历史区、独立卡片组件、悬浮操作和编辑补建会话流程补充前端测试
- [x] 4.3 验证首页现有启动对话框的样式、布局和创建项目流程在引入历史区后保持不变
- [x] 4.4 验证历史记录区的样式与首页风格保持一致，且没有偏离 `taste-skill` 约束方向
