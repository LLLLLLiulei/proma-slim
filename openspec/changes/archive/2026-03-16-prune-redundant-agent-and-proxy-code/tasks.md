## 1. 低风险死代码清理

- [x] 1.1 删除已确认无调用的 renderer 代理请求包装与 `ModeSwitcher` 等死代码入口
- [x] 1.2 删除旧 app-shell / 占位组件 / Chat 残留上下文与原子状态等不可达 renderer 模块
- [x] 1.3 删除未接入当前 Web 运行链路的主进程遗留模块与相关空实现

## 2. 共享导出与依赖收口

- [x] 2.1 清理 `packages/shared` 中已删除功能域的孤儿导出，并同步收口 `config-paths.ts` 中对应旧路径助手
- [x] 2.2 从 `apps/electron/package.json` 删除仅服务于已删除代码的依赖与类型包，并更新锁文件

## 3. 运行时回归验证

- [x] 3.1 确认代理运行时读取、系统探测和 Agent 环境变量注入链路保持可用
- [x] 3.2 运行 `bun run typecheck` 并完成一次关键会话流程验证，确认页面加载、会话发送与消息流未回退
