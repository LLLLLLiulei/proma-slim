## 1. Block-scoped CMS entry

- [ ] 1.1 从 Builder 聊天输入区移除手动 `从 CMS 选择` 主入口，并保留现有区块选择入口与样式语义
- [ ] 1.2 在预览区当前已选区块下方新增独立的浮动操作条组件，第一版仅提供 `从 CMS 选择` 动作
- [ ] 1.3 将区块操作条与现有选区生命周期打通，确保单目标选区、清空选区、预览刷新或重载失效时同步隐藏操作条

## 2. Shared picker and Builder orchestration

- [ ] 2.1 复用现有 `PageBuilderCmsPickerModal`，补充 block-bound 手动模式的标题、说明与默认目标语义
- [ ] 2.2 在 `BuilderPage` 中接入“区块操作条 -> 共享 CMS 选择器”的打开、取消与返回已选区块状态的编排
- [ ] 2.3 保持 Agent 自然语言触发 CMS 选择器的链路可用，并在存在当前已选区块时默认作用于该区块

## 3. Auto-apply and clarification flow

- [ ] 3.1 新增区块级 CMS 确认后的自动应用编排层，复用现有隐藏上下文装饰和 Agent 执行链路
- [ ] 3.2 为自动应用生成一条用户可见的系统代发消息，避免在聊天区暴露 selector 或结构化协议字段
- [ ] 3.3 实现第一版保守的不匹配判断，并仅在必要时通过 `AskUserQuestion` 发起最小必要确认
- [ ] 3.4 打通自动应用成功/失败后的状态清理或保留策略，确保与现有选区和 CMS 选择生命周期一致

## 4. Verification

- [ ] 4.1 补充区块操作条与 Builder 选区交互测试，覆盖展示、隐藏、选区切换与预览失效清理
- [ ] 4.2 补充区块级 CMS 自动应用测试，覆盖确认后自动执行、系统代发消息、取消回退与失败保留
- [ ] 4.3 补充 `AskUserQuestion` 最小确认分支测试，覆盖明显不匹配时的追问与确认后继续执行
- [ ] 4.4 回归自然语言触发 CMS 选择器链路，确认其仍可用且在存在当前选中区块时默认作用于该区块
